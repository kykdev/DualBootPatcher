sudo: required

services:
  - docker

before_install:
  # Clone DualBootPatcher Repository
  - git clone --recursive https://github.com/lazyyq/DualBootPatcher -b manta-oldbase2 DualBootPatcher/ > /dev/null 2>&1
  # Pull docker images
  - docker pull yshalsager/dualbootpatcher:9.3.0-9-base
  - docker pull yshalsager/dualbootpatcher:9.3.0-9-android
  - docker pull yshalsager/dualbootpatcher:9.3.0-9-linux
script:
  # Make work directories 
  - mkdir $HOME/.android
  - mkdir -p ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/ && cd ${TRAVIS_BUILD_DIR}/DualBootPatcher/
  # Build APK
  - |
    docker run --rm -i -e USER_ID=$(id -u) -e GROUP_ID=$(id -g) -v "$(pwd):/builder/DualBootPatcher:rw,z" -v "${HOME}/.android:/builder/.android:rw,z" yshalsager/dualbootpatcher:9.3.0-9-android bash << EOF
    cd DualBootPatcher/builder && ( cmake .. -DMBP_BUILD_TARGET=android -DMBP_BUILD_TYPE=debug && make -j16 && rm -rf assets && cpack && make apk -j16 ) > /dev/null
    ( make android-system_armeabi-v7a -j16 && make -C data/devices -j16 ) > /dev/null
    exit
    EOF
  - |
    docker run --rm -i -e USER_ID=$(id -u) -e GROUP_ID=$(id -g) -v "$(pwd):/builder/DualBootPatcher:rw,z" -v "${HOME}/.android:/builder/.android:rw,z" yshalsager/dualbootpatcher:9.3.0-9-linux bash << EOF
    # Build Utilities Zip
    cd ~/DualBootPatcher/builder && ./utilities/create.sh > /dev/null
    # Build Linux
    ( cmake .. -DMBP_BUILD_TARGET=desktop -DMBP_PORTABLE=ON && make -j16 && cpack ) > /dev/null
    exit
    EOF
after_success:
  - export TRAVIS_CURRENT_DATE=$(date +"%y%m%d-%Hh%Mm")
  # Check output & md5sum
  - ls -l ${TRAVIS_BUILD_DIR}/DualBootPatcher/Android_GUI/build/outputs/apk/debug/Android_GUI-debug.apk
  - md5sum ${TRAVIS_BUILD_DIR}/DualBootPatcher/Android_GUI/build/outputs/apk/debug/Android_GUI-debug.apk
  - ls -l ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/utilities/DualBootUtilities-9.3.0.zip
  - md5sum ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/utilities/DualBootUtilities-9.3.0.zip
  - ls -l ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/DualBootPatcher-9.3.0-Linux.zip
  - md5sum ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/DualBootPatcher-9.3.0-Linux.zip
  # Upload to 0x0.st
  - cd ${TRAVIS_BUILD_DIR}/DualBootPatcher/Android_GUI/build/outputs/apk/debug/ && curl -F'file=@Android_GUI-debug.apk' https://0x0.st
  - cd ${TRAVIS_BUILD_DIR}//DualBootPatcher/builder/utilities/ && curl -F'file=@DualBootUtilities-9.3.0.zip' https://0x0.st
  - cd ${TRAVIS_BUILD_DIR}/DualBootPatcher/builder/ && curl -F'file=@DualBootPatcher-9.3.0-Linux.zip' https://0x0.st
